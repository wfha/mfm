<%= simple_form_for([@store, @hour]) do |f| %>
    <%= f.error_notification %>
    <%= f.input :name %>
    <%= f.input :desc %>
    <%= f.input :open_at %>
    <%= f.input :close_at %>
    <%= f.button :submit, "Submit", class: "btn btn-primary", disable_with: "Saving..." %>
<% end %>