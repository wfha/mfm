<%= simple_form_for(@ticket, validate: true) do |f| %>
    <%= f.error_notification %>
    <%= f.input :email %>
    <%= f.input :content %>
    <%= f.button :submit, disable_with: "Saving..." %>
<% end %>
